{Econet ExternalsAssumes TYPE byte=0..255;word=0..65535;}PROCEDURE Ecoini; EXTERNAL; {Initialise - call once at start of program }PROCEDURE Rxset(VAR blockno:BYTE;port:BYTE;stn,start,length:Word);EXTERNAL;{ Open an RxCB. blockno is updated to contain a number for the block which is then used in calls to Rxread or DelRx. }FUNCTION Rxread(blockno:BYTE;VAR ctrl,port:BYTE;VAR stn,length:Word)  :BOOLEAN;EXTERNAL;{ Returns False unless a packet has been received in the specified block.  If True is returned, the other parameters are updated to give information about the packet, and the RxCB is DELETED.}PROCEDURE Delrx(blockno:BYTE);EXTERNAL;{Delete a specified RxCB explicitly}PROCEDURE Ecotx(ctrl,port:byte;stn,start,length:Word;VAR result:byte);  EXTERNAL;{Transmit a packet. Retries are done for you - <result> returns >127 if OK}